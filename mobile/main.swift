import Cocoa
/*print("请输入用户名\n")
var name = readLine()!;
print("请输入密码\n")
var password = readLine()!;
if(name=="admin"&&password=="888888")
{
    print("access permit");
}
if(name=="admin"&&password != "888888")
{
    print("access denied");
}
if(name != "admin")
{
    print("用户名不正确")
}


 print("请输入用户名")
 var username=readline()!
 print("请输入密码")
 var password=readline()!
 if username == "admin"{
 if password == "888888"
 print("正确")
 else
 print("错误")
 }
 else{
 print("用户名不正确")
 }
 
var index = 0
switch index {
case 100 :
    print("index 的值为 100")
case 10,15 :
    print("index的值为 10 或 15")
case 5:
    print("index的值为 5")
default :
    print("默认 case")
}
*/
/*var sum=0;
var i=0;
while(i<101)
{
    sum = sum+i
    i=i+1 ;
}
print(sum)

var index=1;
var sum=0;
while index <= 100
{
    sum=sum+index;
    index=index + 1;
}
print(sum);

var num = readLine()!
while(num != "q" && num != "")
{
    var number = Int(num)!
    var a = 2 * number
    print(a)
    num = readLine()!
}

var numstr = ""
var num = 0;
while numstr != "q"
{
    numstr = readLine()!
    if(numstr != "q"){
        num = Int(numstr)!
        print("您输入的数字的二倍是:\(num * 2)")
    }
}

var num = readLine()!
var big = 0
while(num != "end")
{
  var number = Int(num)!
    if(big < number)
    {
        big = number;
    }
     num = readLine()!
}
print(big)

var numStr = ""
var num = 0
var max = 0
while numStr != "end"
{
    numStr = readLine()!
    if numStr != "end"
    {
        num = Int(numStr)!
        if num > max
        {
            max = num
        }
    }
}
print("您输入的数字中的最大值为:\(max)")
*/
for index in 1...100{
    print(index)
}
